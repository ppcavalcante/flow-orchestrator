version: "2"

formatters:
  enable:
    - gofmt
    - goimports
    - gci
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(flow-orchestrator)

linters:
  enable:
    - govet
    - errcheck
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - gosec
    - noctx
    - revive
    - misspell
    - prealloc
    - copyloopvar
    - predeclared
    - gocritic
    - nilnil
    - errorlint
    - thelper
    - tparallel

  settings:
    gosec:
      excludes:
        - G204

    govet:
      settings:
        printf:
          funcs:
            - Debugf
            - Infof
            - Warnf
            - Errorf
            - Fatalf

    errcheck:
      check-type-assertions: true
      check-blank: true
      exclude-functions:
        - (*testing.T).Error
        - (*testing.T).Fatal
        - (*testing.T).Fatalf
        - (*testing.T).Errorf

    revive:
      rules:
        - name: exported
          severity: warning
          disabled: false
        - name: unused-parameter
          severity: warning
          disabled: false
        - name: error-strings
          severity: warning
          disabled: false
        - name: error-return
          severity: warning
          disabled: false
        - name: if-return
          severity: warning
          disabled: false
        - name: increment-decrement
          severity: warning
          disabled: false

    errorlint:
      errorf: true
      asserts: true
      comparison: true

  exclusions:
    rules:
      # Exclude all enabled linters for test files
      - path: _test\.go
        linters:
          - gocyclo
          - errcheck
          - gosec
          - unused
          - govet
          - ineffassign
          - revive
          - staticcheck
          - noctx
          - bodyclose
          - gocritic
          - nilnil
          - errorlint
          - thelper
          - tparallel
          - copyloopvar
          - prealloc
          - predeclared
      # Exclude linters for benchmark files
      - path: benchmark
        linters:
          - prealloc
          - errcheck
          - gosec
          - revive
          - unused
          - thelper
          - copyloopvar
          - staticcheck
          - gocritic
      # Exclude linters for example files
      - path: examples/
        linters:
          - gosec
          - revive
          - errcheck
          - staticcheck
          - gocritic
          - predeclared
      # Suppress stuttering type name warnings for public API types
      - text: "stutters"
        linters:
          - revive
    paths:
      - tests/testdata

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
  go: '1.24'
