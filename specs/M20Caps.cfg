\* M20 ph102 concurrency-cap arm — the CORRECT model. CapNeverExceeded (the atomic COUNT-in-txn
\* arbiter, DEC-P98-COUNT-IN-TXN) must HOLD, exhaustive at {w1,w2,w3} x Cap=2.
SPECIFICATION Spec
CONSTANTS
    Workers = {w1, w2, w3}
    Cap = 2
    CapAtomic = TRUE
INVARIANTS
    TypeOK
    CapNeverExceeded
