\* M16 ph77 SEED-THE-BREAK: fencing CAS removed (Fencing = FALSE) → a stale zombie write lands →
\* NoStaleOverwrite MUST Falsify (a counterexample where journal[lvl] < maxWritten[lvl] is reachable).
\* A formal arm whose safety invariant cannot redden is worthless — this proves the fence is load-bearing.
SPECIFICATION Spec
CONSTANTS
    Procs = {p1, p2, p3}
    MaxToken = 4
    Fencing = FALSE
INVARIANTS
    TypeOK
    NoStaleOverwrite
