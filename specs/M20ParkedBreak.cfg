\* M20 ph102 SEED-THE-BREAK: ParkedExempt=FALSE -> a parked parent still counts against the cap ->
\* K parked parents fill the cap, their children can never claim a slot, no parent can resume ->
\* a genuine WEDGE. TLC must report "Deadlock reached" (and NoWedge Falsifies). This is the
\* red-team BLOCKER-1 K-parked-parent deadlock the parked-column dissolved, machine-checked.
SPECIFICATION Spec
CONSTANTS
    Parents = {p1, p2}
    Cap = 1
    ParkedExempt = FALSE
INVARIANTS
    TypeOK
    NoWedge
