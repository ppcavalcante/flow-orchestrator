\* M20 ph102 SEED-THE-BREAK: the atomic count+claim split (CapAtomic = FALSE) -> non-atomic
\* count-then-claim (TOCTOU) -> two workers both claim under a stale count -> running = Cap + 1 ->
\* CapNeverExceeded MUST Falsify. Proves the atomic COUNT-in-txn (not a lagging fenced counter) is
\* load-bearing — the [[engineer-fencing-extends-to-derived-state]] class, machine-checked.
SPECIFICATION Spec
CONSTANTS
    Workers = {w1, w2, w3}
    Cap = 2
    CapAtomic = FALSE
INVARIANTS
    TypeOK
    CapNeverExceeded
