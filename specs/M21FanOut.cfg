\* M21 ph108 fan-out capstone — the CORRECT model. ExactlyNSpawn (expander journaled ONCE,
\* stable N across a kill-storm — the no-replay leg) + FanInWaitsForAll (node terminal iff all N
\* branches terminal) must HOLD, exhaustive at N={b1,b2,b3} x MaxCrashes=2.
SPECIFICATION Spec
CONSTANTS
    Branches = {b1, b2, b3}
    MaxCrashes = 2
    AtomicExpansion = TRUE
INVARIANTS
    TypeOK
    ExactlyNSpawn
    FanInWaitsForAll
