\* M20 ph102 SEED-THE-BREAK: the atomic advance removed (Recheck = FALSE) → the fire's advance is
\* deferred/non-atomic (the fence-as-arbiter fallacy) → a 2nd poller re-enqueues the same due slot →
\* NoDoubleFire MUST Falsify (a reachable state where enqueued[s] = 2). Proves the in-txn re-check +
\* atomic advance is load-bearing — the ph100 double-fire, machine-checked.
SPECIFICATION Spec
CONSTANTS
    Procs = {p1, p2, p3}
    MaxSlot = 3
    Recheck = FALSE
INVARIANTS
    TypeOK
    NoDoubleFire
