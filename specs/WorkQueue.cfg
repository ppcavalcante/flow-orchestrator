\* M17 ph83 formal arm — the CORRECT queue-lifecycle model. All 4 invariants HOLD,
\* exhaustive at {p1,p2} x MaxToken=3. (Ground the state count empirically.)
SPECIFICATION Spec
CONSTANTS
    Procs = {p1, p2}
    MaxToken = 3
    TokenGuard = TRUE
    TerminalReclaimBlocked = TRUE
    ReclaimEnabled = TRUE
    WinnerUnique = TRUE
INVARIANTS
    TypeOK
    NoReclaimOfTerminal
    AtMostOneClaimedWriter
    NoSupersededTerminalize
    NoLostWork
