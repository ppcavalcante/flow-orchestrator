\* M16 ph77 formal arm — the CORRECT fencing model. NoStaleOverwrite (the C4 per-level oracle)
\* must HOLD, exhaustive at {p1,p2,p3} x MaxToken=4 x Levels={0,1} (2809 distinct states, sealed).
SPECIFICATION Spec
CONSTANTS
    Procs = {p1, p2, p3}
    MaxToken = 4
    Fencing = TRUE
INVARIANTS
    TypeOK
    NoStaleOverwrite
